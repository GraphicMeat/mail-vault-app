{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater-macos",
  "description": "macOS updater capability (custom appcast check + Sparkle)",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": ["sparkle-updater:default"]
}
