{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater-macos",
  "description": "macOS updater capability (Sparkle native auto-update)",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": ["sparkle-updater:default"]
}
