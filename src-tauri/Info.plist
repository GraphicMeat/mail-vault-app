<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>SUFeedURL</key>
    <string>https://github.com/GraphicMeat/mail-vault-app/releases/latest/download/appcast.xml</string>
    <key>SUEnableInstallerLauncherService</key>
    <true/>
    <key>SUPublicEDKey</key>
    <string>9OobPkT0p5WGp0wf2aSbADivVllEKZaz0gKhElMocU0=</string>
    <key>SUEnableAutomaticChecks</key>
    <true/>
    <key>SUScheduledCheckInterval</key>
    <integer>86400</integer>
    <!-- macOS malloc otherwise keeps freed pages dirty until the system is
         under memory pressure, so one large header load left the app and its
         daemon (which inherits this environment) at their peak footprint for
         minutes. Measured after syncing a 10,000-message inbox: daemon 107 MB
         -> 15 MB, app 84 MB -> 26 MB. malloc_zone_pressure_relief() does not
         release these pages on macOS 26; this switch does. -->
    <key>LSEnvironment</key>
    <dict>
        <key>MallocSpaceEfficient</key>
        <string>1</string>
    </dict>
    <!-- Mail links elsewhere on the system open MailVault.
         Hand-written on purpose: tauri-plugin-deep-link writes CFBundleURLTypes
         only in its mobile/iOS branch (its build.rs calls
         tauri_plugin::mobile::update_info_plist off `config.mobile`), so on
         desktop macOS the `plugins.deep-link.desktop.schemes` entry in
         tauri.conf.json registers nothing here. Registering the scheme is also
         all we can do: the App Sandbox forbids claiming the default, and this
         build is sandboxed. -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>mailto</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>mailto</string>
            </array>
        </dict>
    </array>
</dict>
</plist>
