<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- App Sandbox -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Network access for IMAP/SMTP. network.server is required for the
         OAuth2 callback loopback listener on 127.0.0.1:19876. -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- File access - user selected files (attachments, exports) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Security-scoped bookmarks for persistent external backup folder access -->
    <key>com.apple.security.files.bookmarks.app-scope</key>
    <true/>

    <!-- Downloads folder access -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- User notifications -->
    <key>com.apple.security.usernotifications.alerts</key>
    <true/>

    <!-- Keychain access for storing passwords (shared with daemon) -->
    <key>com.apple.security.keychain-access-groups</key>
    <array>
        <string>$(AppIdentifierPrefix)com.mailvault.app</string>
        <string>$(AppIdentifierPrefix)group.com.mailvault</string>
    </array>

    <!-- App group for sharing data between app and daemon -->
    <key>com.apple.security.application-groups</key>
    <array>
        <string>group.com.mailvault</string>
    </array>

    <!-- Sparkle auto-updater XPC services (mach-lookup names = bundle-id + suffix) -->
    <key>com.apple.security.temporary-exception.mach-lookup.global-name</key>
    <array>
        <string>com.mailvault.app-spks</string>
        <string>com.mailvault.app-spki</string>
    </array>
</dict>
</plist>
